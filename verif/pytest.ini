[pytest]
addopts = -ra
junit_family = xunit2
markers =
    smoke: minimal boot + insmod + device sanity (one fast boot)
    functional: per-feature guest test binaries, one boot each
    stress: long-running concurrency/integrity stress tests
    matrix: parameterized boot matrix over module parameters
    module: module lifecycle (unload-under-load, reload)
