# Kernel build artifacts
*.o
*.ko
*.mod
*.mod.c
*.mod.o
*.cmd
.*.cmd
*.symvers
*.order
modules.order
Module.symvers
.tmp_versions/
.cache.mk

# Built user-space test/bench binaries
tests/test_producer
tests/test_consumer
tests/test_nonblock
tests/test_poll
bench/*.bin

# QEMU harness build dir (kernel image, initramfs, console log)
.qemu/

# Signing material — NEVER commit the private key
MOK.key
MOK.crt
MOK.der
*.priv

# Editor / OS noise
.vscode/
*.swp
*~
