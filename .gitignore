# Kernel build artifacts
*.o
*.ko
*.mod
*.mod.c
*.mod.o
*.cmd
.*.cmd
*.symvers
*.order
modules.order
Module.symvers
.tmp_versions/
.cache.mk

# Built user-space test/bench binaries (sources are *.c / *.cpp)
tests/*
!tests/*.c
!tests/*.cpp
bench/kbuf_bench
bench/*.bin

# Vendored GoogleTest (fetched by scripts/fetch-googletest.sh)
third_party/

# QEMU harness build dir (kernel image, initramfs, console log)
.qemu/

# Python verification framework
verif/_artifacts/
__pycache__/
.pytest_cache/
.venv/

# Bare-metal benchmark run artifacts
.bench-baremetal.log
.saved-governor

# Signing material — NEVER commit the private key
MOK.key
MOK.crt
MOK.der
*.priv

# Editor / OS noise
.vscode/
*.swp
*~
